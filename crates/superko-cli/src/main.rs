//! The `superko` binary — the operator surface over the search crates.
//!
//! # Status
//!
//! Stub.

fn main() {
    eprintln!("superko: no subcommands yet — the definitions lead. See docs/formal-model.md");
    std::process::exit(2);
}
